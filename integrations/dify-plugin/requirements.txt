# dify-plugin SDK
# Tested with version 0.4.2. Version range is set to allow compatible updates.
# OAuth features (commented out in provider code) require SDK version 0.4.2 or higher.
dify-plugin>=0.4.2,<0.5.0

# AgentCube SDK for code interpreter functionality
agentcube-sdk>=0.0.10
